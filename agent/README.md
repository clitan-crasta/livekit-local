# Collect Agent

LiveKit voice agent: collects name, age, place; POSTs to localhost:4000/save; says thank you and ends the call.

Run: `uv run agent.py dev` (after `uv run agent.py download-files` once).
